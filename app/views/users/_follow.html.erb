 <%= form_for(current_user.following_relationships.build, remote: true) do |form| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <% form.submit do %>
    <div class="btn btn-primary">Follow</div>
  <% end %>
<% end %>