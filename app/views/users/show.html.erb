<p id="notice"><%= notice %></p>

<%= @user.email %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

<div>
  <table>
    <tbody>
      <tr>
        <th>Endorsements:</th>
      </tr>
      <%= render partial: 'endorsement', collection: @endorsements unless @endorsements.empty? %>
    </tbody>
  </table>
</div>
<div>
  <% if @endorsements.empty? %>
      This user currently has no endorsements.
  <% end %>
</div>
